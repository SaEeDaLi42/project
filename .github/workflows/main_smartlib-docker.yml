# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - smartlib-docker

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://smartlibregistry.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_43cca5c8a08a44a6a4058560e6d22d08 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_b27a528db26a4a499cd8e6828b27d9a4 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: smartlibregistry.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_43cca5c8a08a44a6a4058560e6d22d08 }}/smartlibregistry/smartlibregistry.azurecr.io/smartlib-app:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'smartlib-docker'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_d32b3f827bcf4bca96190bbe123db81a }}
        images: 'smartlibregistry.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_43cca5c8a08a44a6a4058560e6d22d08 }}/smartlibregistry/smartlibregistry.azurecr.io/smartlib-app:${{ github.sha }}'